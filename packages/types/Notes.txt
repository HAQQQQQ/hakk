---In order to publish this package to the npm registry---
1) cd ./hakk/packages/types 
2) update types package.json version 
3) npm publish
4) Update server package.json so that @hakk/types points to most recent version